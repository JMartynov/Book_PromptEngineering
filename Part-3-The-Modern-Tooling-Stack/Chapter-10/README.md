# Chapter 10: Vector Databases & RAG

This chapter introduces the "Storage Layer" of the modern AI stack.

## Key Takeaways:
- Vector DBs as the **Long-Term Memory** of an AI system.
- The shift to **Hybrid Search** (Vector + Keyword + Metadata).
- Deep technical dive into **Vector Embeddings**, **Hybrid Search**, and **Cross-Encoder Reranking**.
- 8 real-world Python examples illustrating:
  - Basic **Semantic Search** logic using local embedding models.
  - Hybrid search with metadata-based **Security Filters**.
  - Text **Chunking with Overlap** for context preservation.
  - **Self-Querying** using natural-to-structured filter generation.
  - **Reranking** with cross-encoders for 10x higher accuracy.
  - **Small-to-Big** (Parent Document) retrieval patterns.
  - **Multi-Modal** (text + image) vector indexing.
  - Evaluating RAG pipelines with **RAGAS** metrics.
